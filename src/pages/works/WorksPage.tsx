import React from 'react';
import WorkPostContainer from '../../components/works/WorkPostContainer copy';

const WorksPage = () => {
  return (
    <section className='pb-2'>
      <WorkPostContainer />
    </section>
  );
};

export default WorksPage;
